import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:serial_terminal/app/modules/bluetooth/bluetooth_page.dart';

main() {
  testWidgets('BluetoothPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BluetoothPage(title: 'Bluetooth')));
    //  final titleFinder = find.text('Bluetooth');
    //  expect(titleFinder, findsOneWidget);
  });
}
