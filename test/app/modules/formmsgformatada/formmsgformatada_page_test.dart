import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:serial_terminal/app/modules/formmsgformatada/formmsgformatada_page.dart';

main() {
  testWidgets('FormmsgformatadaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FormmsgformatadaPage(title: 'Formmsgformatada')));
    //  final titleFinder = find.text('Formmsgformatada');
    //  expect(titleFinder, findsOneWidget);
  });
}
