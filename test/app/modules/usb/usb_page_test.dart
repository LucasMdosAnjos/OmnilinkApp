import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:serial_terminal/app/modules/usb/usb_page.dart';

main() {
  testWidgets('UsbPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UsbPage(title: 'Usb')));
    //  final titleFinder = find.text('Usb');
    //  expect(titleFinder, findsOneWidget);
  });
}
